# Chronicle — Cross-Session Sync Log

Both Claude Code sessions (frontend, backend) read this file at the start of every cycle and append a 2-line summary at the end of every cycle.

## Format

Each entry, newest at the bottom:

```
Cycle N — [session]: [what was built], [issues if any]
```

Where:
- `N` is the global cycle counter (frontend cycle 1, backend cycle 1, frontend cycle 2, ... — interleaved by completion order)
- `[session]` is `frontend` or `backend`
- `[what was built]` is a one-line summary of what shipped this cycle
- `[issues if any]` is a one-line note of any breakage, blockers, or unresolved API mismatches surfaced this cycle

## Integration cycles

Every 3-4 work cycles, both sessions stop. Reconciliation step:
1. Verify the API contract in [API.md](API.md) still holds (frontend's expected response shape == backend's actual response shape).
2. Fix any mismatches.
3. Append an entry: `Integration N — both: API contract verified, [issues if any], resumed`.

## Locked at H0

The first entry below is the planner handoff. Everything after that is session-generated.

---

## Log

Cycle 0 — planner: docs restructured for parallel sessions (BRIEF, schema, API, MOCK_DATA, FRONTEND-STANDARDS, BACKEND-STANDARDS, STATE), MOCK_DATA fixtures generated for Cases 1+2 + Case 3 shape-mock, route restructure landed (/ → landing, /app → product). Frontend and backend sessions can begin once Murat reviews docs/BACKEND-STANDARDS.md OPEN ITEMS.

Cycle 0.1 — planner: BACKEND-STANDARDS.md OPEN ITEMS resolved (Voyage proceed-as-is, Gemini @google/generative-ai SDK, 10MB PDF cap, pLimit(8), console-only telemetry); added J.10 (body parser via formData() + AbortSignal propagation across Anthropic/Voyage/Gemini calls). BUILD.md H10 polish + Block 7 color decision updated to the new locked palette per FRONTEND-STANDARDS.md §H.1 supersession. All docs verified consistent. Frontend and backend sessions cleared to start.

Cycle 1 — backend: built lib/schema.ts (zod + TS types) + lib/sse.ts (frames + heartbeat) + lib/claude.ts (Sonnet 4.6 extraction, no citations per Block 5b finding) + app/api/extract/route.ts (formData + pLimit(8) + SSE), docs amended to drop Citations API per Block 5b verification fallback (BUILD.md Risk 1 path). [issues: model picks weak snippets and violates `values` schema — both deferred to H9 prompt iteration]

Cycle 2 — backend: built prompts/few_shot.md (2 few-shots covering 7/7 event types, Cases 1+2 only) + wired into lib/claude.ts. [issues: none — live re-run on d1_pcp_2023_01.pdf confirmed both H8-H9 deferred issues resolved (visit snippet now HPI not header, non-lab values now null, lab values single-analyte shape, cache hit on 5916 tokens)]

Cycle 3 — pre-H0 housekeeping: held_out/case3/ scaffold landed (README with deferred-status banner, ground_truth.json template stub, docs/.gitkeep). Case 3 PDFs (Block 1) + GT labels (Block 2) consciously deferred per Murat to H8-deadline (RESOLVED-DECISIONS §9). At H8 fork: Path A (labeling done → normal H8-H9 prompt iteration → demo beat 3 works) or Path B (labeling not done → freeze prompt, skip H8-H9 iteration, demo becomes 3-beat). [issues: H7→H8 cycle MUST add a `gt_not_present` graceful-degrade conditional to app/api/eval/route.ts (~5 lines) so missing GT returns a friendly SSE error frame instead of crashing — flag for whoever picks up cycle 4]

Cycle 4 — backend: re-verified cycle-1 deliverables (lib/schema.ts, lib/sse.ts, lib/claude.ts, app/api/extract/route.ts) against current spec — all four files align with PLAN.md Q1-amended (Citations dropped per Block 5b), schema.md, BACKEND-STANDARDS J.2/J.7/J.10. tsc --noEmit clean. Cached fixture replay against MOCK_DATA.md case1 (13 events, all 4 event_types present in case1: visit/lab/diagnosis/medication) round-trips cleanly through CaseFixtureSchema + TimelineEventSchema; sseEvent frame shapes well-formed; values-discipline holds (lab populated, non-lab null). [issues: none new — Cycle 3's flagged `gt_not_present` graceful-degrade for app/api/eval/route.ts still pending for whoever owns the H7→H8 hour]

Cycle 5 — backend: built lib/normalize.ts + lib/match.ts with co-located node:test suites (21/21 pass), implementing the verbatim-snippet match algorithm per BUILD.md H2-H3 spec and schema.md hard-constraint #1. Coverage includes 3 real-fixture matcher assertions sourced from MOCK_DATA case1 d1 (visit/lab/medication snippets vs prompts/few_shot.md text excerpt), a one-character-substitution negative test, and edge cases (empty inputs, smaller-window preference, degenerate small-doc fallback, snippet-longer-than-30-spans cap, dehyphenation across line break). normalize() preserves case (matcher case-folds at containment time) per spec. tsc --noEmit clean; npm run lint clean for new files (8 errors / 1470 warnings are all pre-existing baseline in scripts/ + components/). package.json gained one `test` script line. [issues: none new — Cycle 3's flagged `gt_not_present` graceful-degrade for app/api/eval/route.ts still pending for whoever owns H7→H8]

Cycle 6 — backend: built scripts/extract-case.ts (CLI dump cases 1+2 to events.json), app/api/cases/[id]/events/route.ts (cached read), and replaced the 501 stub in app/api/extract/route.ts with full SSE replay (1.5s per-doc feel-delay per Q20). [issues: Cycle 3's `gt_not_present` graceful-degrade for app/api/eval/route.ts still pending — carried forward to whoever owns H7→H8]

Cycle 7 — backend: built lib/eval.ts (greedy 1-1 matching + per-tier evaluate + per-event-type breakdown, all pure) with co-located node:test suite (12 new + 21 baseline = 33/33 pass), derived data/cases/case[12]/ground_truth.json from MOCK_DATA fixtures, app/api/eval/route.ts (cached mode for Cases 1+2 + live SSE mode for Case 3 with gt_not_present / gt_hash_mismatch / prompt_dirty graceful-degrades all in place), scripts/eval-train.ts (reads predictions + GT, writes data/eval_reports/<case>.json, appends to prompts/CHANGELOG.md). Path B is active (Case 3 PDFs+GT not written) → live mode currently short-circuits with gt_not_present per cycle 3's flag — flag now resolved. tsc --noEmit clean; lint clean for new files. eval-train run on Cases 1+2 yields C1 strict P=0.38 R=0.77 F1=0.51 / loose P=0.35 R=0.75 F1=0.47 and C2 strict P=0.18 R=0.75 F1=0.29 / loose P=0.18 R=0.75 F1=0.29 — surfaces real over-extraction in v1 prompt (deferred to H8 iteration if Path A activates). [issues: none new]

Cycle 8 — backend (bugfix): cycle 7 surfaced loose F1 < strict F1 on case1 (0.47 < 0.51) — mathematically impossible under monotone matching. Root-caused to two issues: (a) lib/claude.ts didn't re-stamp model-generated event ids, and Sonnet 4.6 emits non-unique ids across docs (case1 cycle-6 run had 26 events / 17 unique ids; collisions on the `c3d4e5f6-a7b8` and `a1b2c3d4-e5f6` prefix families); (b) lib/eval.ts tracked matched preds via `Set<string>` keyed on pred.id, undercounting tp when ids collided. Fixed: lib/claude.ts now re-stamps every event's id to crypto.randomUUID() post-validation; lib/eval.ts tracks matchedPredIdx as Set<number> indexed on prediction array position (defensive — robust regardless of upstream id discipline). Re-extracted cases 1+2 (now 25 + 16 events, all uniqueIds=eventsCount) and re-ran eval-train. New metrics: C1 strict P=0.40 R=0.77 F1=0.53 / loose P=0.40 R=0.77 F1=0.53; C2 strict P=0.44 R=0.88 F1=0.58 / loose P=0.44 R=0.88 F1=0.58 — monotone (loose ≥ strict in all tiers/cases) restored. tsc + 33/33 tests still pass. One incidental: case1 d7 dropped one event during re-extraction (model emitted `values.ref_range: null`, violating schema's "all sub-fields required when values non-null" rule — event correctly dropped via cycle-1's per-event safe-parse, not a regression). [issues: none new — Cycle 3's `gt_not_present` flag was resolved in cycle 7]

Cycle 9 — backend: built lib/gemini.ts (Gemini Flash streaming explainer per Q26 + AbortSignal honor per J.10; lazy GoogleGenerativeAI client mirroring lib/claude.ts getClient pattern; MODEL=gemini-2.5-flash; system prompt verbatim from API.md passed via systemInstruction; serializeEvent factored out + exported so the Haiku fallback in the route shares the identical user prompt) and app/api/explain/route.ts (SSE endpoint per API.md, runtime=nodejs, zod-validated body, Gemini primary with Haiku 4.5 inline fallback — claude-haiku-4-5-20251001, max_tokens=400, messages.stream iterating content_block_delta / text_delta events). Fallback fires on missing GEMINI_API_KEY OR any Gemini error before Gemini produced its first chunk; if Gemini already streamed text and then failed mid-flight the route closes with `done` rather than restarting Haiku and risking a duplicated answer; both providers failing emits a single `error` frame with code=upstream_unavailable, retryable=true. lib/gemini.test.ts covers 6 cases (no real API calls — system-prompt-verbatim check + serializer with full+sparse events + async-iterable shape + already-aborted no-yield + missing-key throw). Structural smoke harness (in-process Request → POST, with require-cache stubs for both providers; deleted before completion) confirmed: bad-body → 400 + standard envelope (code=pdf_invalid); stubbed Gemini yielding ["Hello"," ","world"] produces exactly `data: {"type":"token","text":"Hello"}\n\ndata: {"type":"token","text":" "}\n\ndata: {"type":"token","text":"world"}\n\ndata: {"type":"done"}\n\n`; Gemini-throws-first-chunk + stubbed Haiku confirmed Haiku takes over and emits its own tokens (log line `[explain] gemini_failed event_id=ev1 produced=0 reason=...` followed by `[haiku] event_id=... tokens=2`). tsc --noEmit clean; npm test 39/39 (33 baseline + 6 new); lint clean for new files. [issues: none new]

Cycle 10 — backend: built lib/voyage.ts (batch embed via Voyage voyage-3 HTTP per Q3 + single-request batching per J.3 + OpenAI text-embedding-3-small fallback on 401/429/network/malformed-response per Q3 + cosine helper + eventEmbedText helper for "title — summary" format) and app/api/related/route.ts (POST handler per API.md, runtime=nodejs, zod-validated body, target found within candidates list else 400, target+others embedded in one batched call, top-3 with score >= 0.55 sorted desc, scores rounded to 2dp, cached: false literal per spec, 502 upstream_unavailable on both-providers-fail, AbortSignal threaded through). No SDKs added — Node fetch only. Lazy env-var check throws only when BOTH keys missing; index-sort defensive on every provider response (neither guarantees response order). AbortError re-thrown without fallback so caller cancellation never triggers a redundant OpenAI call. lib/voyage.test.ts covers 14 cases (no real API calls — globalThis.fetch stubbed: cosine identical/orthogonal/opposite/zero, empty-input no-call, response-index-shuffle handling, Voyage→OpenAI fallback on 429 + network error + malformed response + voyage-only-key + openai-only-key, both-providers-fail throw, both-keys-missing throw, AbortSignal verbatim pass-through, AbortError-no-fallback). Structural smoke harness (in-process Request → POST, fetch globally stubbed, deleted before completion) confirmed all 22 invariants: bad-body → 400 + envelope (code=pdf_invalid, retryable=false); missing-eventId → 400 with "not present" message; happy path with hand-crafted vectors (target=[1,0], A/B/C/D scoring 1.0/0.857/0.707/0.447) returned exactly [a,b,c] sorted desc with D (below 0.55) excluded, scores 2dp-rounded, single HTTP call; Voyage 429 → OpenAI fallback flowed end-to-end through the route (log lines `[voyage] fallback_to_openai reason=voyage_http_429` then `[related] target=tgt candidates=1 top=1 provider=openai`); both-providers-500 → 502 + code=upstream_unavailable + retryable=true. tsc --noEmit clean; npm test 53/53 (39 baseline + 14 new); lint clean for new files (8/1470 baseline unchanged). [issues: none new]

Phase 2 closed — backend complete at v0.2.0 (commit 392ce2d, PR #6). All 5 routes verified end-to-end via dev-server walkthrough (10/10 checks, including Path B `gt_not_present` short-circuit at 12ms wall, cached SSE replay at 10.5s = 7×1.5s feel-delay exactly, real-API smokes against Anthropic Sonnet 4.6 + Gemini 2.5 Flash + Voyage voyage-3). Frontend session next: zero product UI built (only `app/page.tsx` landing + `app/layout.tsx` exist; no `/app/page.tsx`, no `/eval/page.tsx`, no dropzone, timeline, side panel, event-card, or pdf-viewer). Pick up at BUILD.md H1→H2 (shadcn shell + dropzone) — backend API contract is locked and won't change.

Cycle 11 — frontend: cycle 1 scaffold landed. app/page.tsx rebuilt as the 5-section landing per FRONTEND-STANDARDS.md §I (hero with placeholder-hero.png + TODO comment + CSS-only yellow snippet pulse honoring prefers-reduced-motion; problem stat cards; 3-step "Drop/Read/See" with placeholder icon tiles + lucide TODO; trust two-column with paper-textured PDF mock + highlighted snippet + "What does this mean?" card; final CTA + footer attribution). app/app/page.tsx scaffolded — header (Chronicle wordmark linking to /), 3 preset buttons (case1/2/3), react-dropzone (PDF-only, multi-file), bullet-list event rendering wired to MOCK_DATA case1 fixture (lib/fixtures.ts) with severity dot + date-confidence formatting + source citation per event; case2/case3 buttons land empty-state for now (follow-up cycle); ad-hoc drop falls back to case1 fixture so the rendering path stays exercised until /api/extract wiring at H2. components/disclaimer-footer.tsx (locked Q21 copy + optional /eval link) and components/splash-disclaimer.tsx (one-time via localStorage flag chronicle.splashDismissed.v1) created. Design tokens: globals.css overrode shadcn :root vars to the locked palette (#FAFAF7 base / #0F766E accent / #E5E5E0 borders / #DC2626 destructive) so shadcn primitives don't bleed, and added Chronicle-specific Tailwind tokens (bg-base, bg-surface, text-ink/ink-muted/ink-subtle, border-line, accent-teal, snippet) under @theme inline. Inter + Source Serif 4 wired via next/font. Generated a 1100x687 #F5F5F0 PNG at public/placeholder-hero.png so /placeholder-hero.png resolves. tsc --noEmit clean; next build green (Turbopack 1443ms compile, 9/9 static pages including / and /app). [issues: case2/3 fixtures + real /api/extract+/api/cases SSE wiring still owed (H2 work); placeholder-hero.png is a flat color — swap to real /app screenshot at H10/H11; "Drop/Read/See" icon tiles use letter glyphs, swap to lucide Upload/ScanText/LineChart once import names confirmed]

Cycle 12 — backend (H8→H9 prompt v2 + v3): wrote prompts/system_extract_v2.md (+preamble explaining the v1→v2 single-rule change) and updated the inlined SYSTEM_PROMPT body in lib/claude.ts plus the three hardcoded "system_extract_v1.md" references in scripts/extract-case.ts + scripts/eval-train.ts (path + promptVersion field + CHANGELOG `v1` literal → `v2`). v2's ONE targeted change per EVAL.md discipline: replaced v1's "If a document mentions an event that occurred BEFORE this document's date, emit it as a separate event with the referenced date" rule with a Primary-source rule — emit only events the current document is the primary source of (the doc that ordered/performed/decided/originally reported), do NOT emit events for cross-document references like "prior A1c was 9.2%" or "PMH notable for T2D". Targeted at the dominant FP pattern surfaced by cycle-7/cycle-8 metrics: ~12 of ~21 false positives across both cases were cross-doc-reference duplicates (5/8 C1 lab FPs were "Prior HbA1c — referenced" entries, 3/4 C2 imaging FPs were the d2 mammogram re-emitted from d3/d4/d5). Re-extracted both cases (C1 25→18 events, C2 16→8 events; cache_create=6243 once on the first v2 call then cache_read=6243 across all subsequent calls — cache invalidation + repopulation working correctly under v2's new prompt body, hash f1a9472). Re-ran eval-train. Result mixed but net positive: C1 strict F1 0.53→0.45 (regression −8pt; lab FP cut 8→4 ✓ but lost 1 lab to title-overlap edge case + 1 medication to GT-disagreement on d5's "continue 850mg" recap which is a doc-author dose-discrepancy GT counts but the document phrases as unchanged), C2 strict F1 0.58→0.88 (+30pt; imaging FP cut 4→0 ✓ exactly the predicted win, P=R=0.88 hits both EVAL.md ≥0.85 stop-condition thresholds). Average strict F1 0.555→0.665, +11pt. C2 at stop-condition. C1 regression revealed the next-largest failure mode independent of v2's change: visit titles. Model emits richer/more-specific titles ("PCP annual physical — new symptoms reported", "Diabetes follow-up visit — well controlled") vs GT's terse style ("PCP visit — initial workup", "PCP follow-up — lifestyle counseling") — token overlap 0.17–0.43, all below the 0.5 matcher threshold; all 4 C1 visit matches fail this way; v1 had this same problem (visit tp=1/4) and v2 went 0/4. Pure title-style mismatch, not a content disagreement, and explains the bulk of the C1 regression. CHANGELOG v2 line appended with f1a9472 hash + iso timestamp. Per EVAL.md prompt-iteration discipline: ONE change per version → v2 logged as-is; v3 (next session if Murat continues iteration) should target the visit-title format issue (e.g., add explicit Title-format examples for visit type to bias the model toward the GT-aligned terse "specialty + visit-type" pattern over rich-narrative titles). Stop conditions not yet hit: C1 still below 0.7 P/R; C2 hit 0.85+; not 3 consecutive flat versions; iteration time well under 90 min. Murat's call whether to continue with v3 or freeze v2 — chose to continue. [issues: none new — the d5 metformin-continuation case is a labeling judgment call (GT counts a doc-author dose discrepancy as an event despite the document phrasing it as unchanged); could be re-labeled in_scope:false in case1/ground_truth.json to remove that one penalty, but per EVAL.md "do not modify GT after labeling" discipline that's a non-starter — the metric is the metric]

v3 (same cycle, immediately after v2): wrote prompts/system_extract_v3.md, updated lib/claude.ts SYSTEM_PROMPT inline + the three "system_extract_v2.md" references in scripts/extract-case.ts and scripts/eval-train.ts (path + promptVersion + CHANGELOG `v2`→`v3` literal). v3's ONE targeted change per EVAL.md discipline: added explicit terse visit-title format guidance to the inline prompt. Audit-trail finding surfaced en route: the per-type Title format conventions documented in v1.md / v2.md (e.g., the visit template "<specialty> visit — <chief complaint or focus>") were NEVER in the actual inlined SYSTEM_PROMPT — the inline copy collapsed the entire Title section to "A short scannable headline." across v1 and v2, so under v1 and v2 the model produced rich visit titles based on its own judgment with zero inline format guidance, and the .md files documented an aspirational format the prompt never delivered. v3 closes this gap for visits only (the bottleneck event type per the cycle-12-v2 eval): inlined a 4-template ladder ("<specialty> visit — <2-3 word purpose>" / "<specialty> follow-up — <2-3 word purpose>" / "<specialty> consult" / "Annual physical exam") with explicit "do NOT pile on adjectives from the chief complaint" anti-pattern. Other 6 event types still follow model judgment. Re-extracted both cases (C1 18 events held, C2 8 events held; cache_create=6503 once on v3's first call then cache_read=6503 across all subsequent calls). Re-ran eval-train. Result: C1 strict F1 0.45→0.65 (+20pt — visit FP/FN both dropped to 0, all 4 visits matched, the matcher receiving exact "PCP visit — initial workup", "PCP follow-up — diabetes management" ×2, "PCP annual physical" titles), C2 strict F1 0.88→0.88 (held — C2 visit titles already worked under v2 because the d1/d4/d6 model emissions kept their OB/GYN / Breast surgery prefix, so the v3 narrowing didn't change them). Avg strict F1 over the iteration: v1 baseline 0.555 → v2 0.665 → v3 0.765 (+21pt absolute over 2 versions). Per EVAL.md stop conditions: C2 hits ≥0.85 P/R; C1 strict P=0.56 / R=0.77 (above 0.5 floor, R past 0.7 floor, neither at 0.85 ideal); not 3 consecutive flat versions; iteration time well under 90 min. Diminishing-returns analysis on remaining C1 FPs (3 panel-split + 2 lingering cross-doc lab references + 3 valid-but-not-in-GT referrals) and the 1 unrecoverable medication FN (d5 "Continue 850mg" doc-author discrepancy) suggests v4 has marginal expected value — recommend freeze at v3. CHANGELOG v3 line appended with 455c97b hash. tsc --noEmit clean. [issues: none new]

v4 (same cycle, immediately after v3 — Murat continued past the v3 freeze recommendation): wrote prompts/system_extract_v4.md with audit-trail-divergence-closing preamble, updated lib/claude.ts SYSTEM_PROMPT inline + the three "system_extract_v3.md" references in scripts. v4's ONE targeted change per EVAL.md discipline: inlined the full per-type Title format spec for the 6 non-visit event types (lab "<test name> — <result> <unit>" / imaging "<modality> <body part> — <one-line finding>" / diagnosis "Dx added: <condition>" with explicit "not a workup impression" note / medication "<verb> <drug name> <dose>" / procedure "<procedure name>" / referral "Referral to <specialty>"), closing the v3-surfaced divergence where the .md spec documented per-type formats that never reached the model. Re-extracted both cases (C1 18 events held, C2 8 events held; cache_create=6852 once on v4's first call then cache_read=6852 across all subsequent calls — cache invalidation + repopulation working under the new ~350-token Title section addition). Re-ran eval-train. Result exceeded expectation: C1 strict F1 0.65→0.77 (+12pt — recall jumped 0.77→0.92 because the inlined "HbA1c — <result> %" format produced GT-exact lab titles like "HbA1c — 9.2 %" matching the GT verbatim, recovering the lab events v3 lost on title-overlap edge cases; precision also improved 0.56→0.67 as the diagnosis "Dx added:" prefix biased the model away from emitting workup impressions as diagnosis events), C2 strict F1 0.88→0.88 (held — C2 was already at stop threshold, no new format gains, no regression). Avg strict F1 over the full iteration: v1 baseline 0.555 → v2 0.665 → v3 0.765 → v4 0.825 (+27pt absolute over 3 versions, all gains preserved). Per EVAL.md stop conditions: C1 strict R=0.92 (well past 0.7 floor), P=0.67 (above 0.5 floor, capped here by 3 GT-labeling-judgment referral FPs the model emits correctly + 2 panel-split lab FPs that can't be sharpened without risking the legitimate d6 dual-analyte panel + 1 cross-doc lab dup edge case + 1 unrecoverable medication FN). C2 P=R=0.88 maintained. Genuine diminishing returns: v5 would have to either (a) sharpen panel-collapse rule risking d6 LDL drop or (b) reinforce primary-source rule risking C1/C2 recall regression, both negative-expected-value moves. CHANGELOG v4 line appended with f32ebd0 hash. tsc --noEmit clean. **PROMPT FROZEN AT v4.** Active lib/claude.ts inline SYSTEM_PROMPT delivers full Title format spec for all 7 event types + primary-source rule + verbatim-snippet rule + severity guidance + value-discipline + refusal handling, all consistent with prompts/system_extract_v4.md. CHANGELOG.md captures the full v1→v4 paper trail for the demo's "labeled before iterating + iterated against Cases 1+2 only" credibility narrative. The cycle-13/14 frontend SSE consumer (cycle 14 wired through `POST /api/extract` cached preset) will pick up the v4 events.json automatically — no frontend re-work required. Path B is still active for Case 3 (PDFs+GT not yet authored by Murat). [issues: none new]

Cycle 13 — frontend (cycle-11 follow-ups + animation pass): closed two cycle-11 carry-forwards and added the H.3 animation budget. lib/fixtures.ts gained CASE2_FIXTURE (8 events from MOCK_DATA case2 — Maria Rodriguez OB/GYN→mammogram→biopsy chain) and CASE3_FIXTURE (6 mock-only events with `mock_only_not_eval_data: true` flag preserved per MOCK_DATA discipline; never used as eval input), plus a FIXTURES index keyed on caseId. app/app/page.tsx preset switch now wires all 3 buttons through `FIXTURES[id]`; ad-hoc drop continues to fall back to case1 fixture pending H2 /api/extract wiring. Per-event icons land via lucide-react: FlaskConical/ScanLine/Stethoscope/ClipboardList/Pill/Activity/ArrowRightLeft (Q7 mapping per RESOLVED-DECISIONS #4), with FileText as defensive fallback. 4px severity-colored left-edge bar added to each event card per Q10 (color-only on timeline; severity text-label still pending side-panel work at H6). Landing "Drop/Read/See" tiles swapped from letter glyphs to lucide Upload/Eye/LineChart per FRONTEND-STANDARDS §I Section 3. Framer Motion entrance animations honoring useReducedMotion across both surfaces: landing hero cascade (headline immediate + subhead +0.15s + CTA +0.3s + product preview +0.5s @ 0.8s duration) per H.3 §2; SectionEntrance helper using whileInView fade-up (opacity 0→1, y 20→0, duration 0.6s, ease cubic-bezier(0.16,1,0.3,1), viewport.once + margin -100px, 100ms sibling stagger) wraps Sections 2-5 headings + cards per H.3 §1; /app event list now uses motion.li with layout + per-item delay clamped to ≤0.4s for the staggered fade-in. Button hover lifts via Tailwind transitions (translate-y-px + softer shadow growth) on the two CTAs and 3 preset cards. Magic MCP registered at user scope (`claude mcp add magic --scope user --env API_KEY=… -- npx -y @21st-dev/magic@latest`; `claude mcp list` reports `magic: ✓ Connected`) but the tool registry baked at session start does NOT include `mcp__magic__*` — confirmed via ToolSearch returning zero matches. Polish pass therefore went hand-written within the H.3 budget; FRONTEND-STANDARDS §H.2 step-1 (Magic MCP first) deferred to a fresh session post-/exit. tsc --noEmit clean; next build green (Turbopack 1443ms compile, 9/9 static pages). Dev server (background task bci1sxvcq) stopped at end of cycle. [issues: real /api/extract + /api/cases SSE wiring still owed — H2 work; placeholder-hero.png is a flat 1100x687 #F5F5F0 PNG, swap at H10/H11; the remaining cycle-11 issue (lucide event-type + landing icons) resolved this cycle; new: a re-launched session should drive landing hero polish through Magic MCP `/ui` per H.2 (10-call ceiling, 8 soft target, locked-token adaptation pass mandatory before merge)]

Cycle 15 — frontend (H3 + H4 + H6 polish + H7 + H8 + H10 + landing rewrite + scroll-reveal block): single mega-cycle that closed every frontend BUILD.md hour from H3 through H10, plus a Linear-grade landing rewrite and a Magic MCP scroll-reveal block. Components shipped: components/pdf-viewer.tsx (react-pdf 10.x with text-layer; on onRenderTextLayerSuccess collects spans → matchSnippet from lib/match.ts → wraps matched run with snippet-highlight token #FEF08A60% + scrollIntoView; fallback yellow banner with snippet text when no match; Blob → URL.createObjectURL with revoke on cleanup; worker path /pdf.worker.min.mjs from public/; SSR-disabled via next/dynamic in caller), components/side-panel.tsx (shadcn Sheet slide-over with severity badge + title + date/provider + summary + source-line + ExplainerSection + PdfViewer + RelatedSection — H6 polish + H7 explainer + H10 related all colocated; click-outside / ESC close via Sheet defaults), components/event-card.tsx (4px severity edge bar + lucide event-type icon + date/type captions + title/summary/source-line; click → onSelect; teal-bordered active state), components/timeline.tsx (vertical 2px center axis with severity-colored axis dots; cards alternate left/right on 12-col grid even=L odd=R; AnimatePresence + per-card motion.li with layout + initial fade-in offset clamped to 0.4s delay; useReducedMotion respected; H6 axis-dot CSS-only hover tooltip showing "Severity: <Label> — <hint>" via group-hover/dot pattern + native title attribute as accessibility floor). New routes: app/api/cases/[id]/docs/[docId]/route.ts (GET, streams data/cases/<id>/docs/<docId>.pdf with application/pdf + 5min cache; defense-in-depth path-traversal guard via regex on docId + resolved-path containment check; 404 envelope on missing). Hook extension: lib/use-extraction-stream.ts gained Map<docId, File> + getDroppedFile() so the side panel can preview ad-hoc-dropped PDFs; state.caseId + state.mode plumbed so the panel knows whether to fetch from /api/cases/<id>/docs/<docId> (cached presets) or read the in-memory blob (drops). H7 explainer: ExplainerSection in side panel — "Explain in plain language" button (Sparkles icon, teal accent), POST /api/explain with { eventId, event }, consumes SSE token frames into a "Patient view · generating" card with spinner, "Re-explain" button, AbortController on event change. H8 /eval: app/eval/page.tsx — two tabs (Live held-out · default / Cached Cases 1+2), tab switcher + sub-eyebrow per Linear pattern, LivePanel auto-runs GET /api/eval?case=case3&mode=live on mount with a custom SSE consumer that handles started/doc_started/doc_complete/doc_error/metric/breakdown/error/done frames and graceful-degrade copy for gt_not_present (Path B), gt_hash_mismatch, prompt_dirty, fallback_placeholder error codes; CachedPanel toggles between case1/case2 via /api/eval?case=...&mode=cached and renders the same Report component; Report = two TierCards (P/R/F1 + tp/fp/fn/n_gt mono row) + per-event-type breakdown table (n_gt + strict P/R/F1 + loose P/R/F1, 7 event types in canonical order, em-dash for n_gt=0 rows) + collapsible Methodology details with strict/loose definition + GT lock note + active prompt version/hash + footer caption with timestamp; Cmd+Shift+L hotkey listener swaps to /data/case3_eval_fallback.json (recognizes _placeholder:true and surfaces "still placeholder" copy until H11). H10 related: RelatedSection in side panel — auto-fetches POST /api/related with { eventId, candidates: allEvents } on event change, renders top-3 mini-cards under PDF viewer with score% in teal mono + title + date/type + click → switches panel to that event (full state reset); silent on 502 or empty results. /app rewired: replaced inline event list with <Timeline> + <SidePanel>; selectedEvent in component state; all panel concerns plumbed (caseId, allEvents, getDroppedFile, onSelect, onClose). Landing rewrite (app/page.tsx): full restructure to Linear-grade rigor — sticky top nav with single hairline + ink-filled "Open app" CTA + "Evaluation" link; hero with mono eyebrow + clamp(56px,9vw,104px) headline at -0.035em + 19px subhead on 44ch measure + filled "Get started →" + text-only "See evaluation ↗" + AppMockup hand-built representation of /app (browser chrome reading chronicle.app/app — Sarah Chen, 47F + 7-col event list with 6 stylized cards + 5-col side panel with severity badge + title + provider + summary + faux PDF source preview where the snippet pulse animation now lives on the actual verbatim quote); four numbered SectionDividers (01 Problem / 02 Method / 03 Trust / 04 Start) with full-width hairlines + 2-col mono-marker layout; Problem reflowed as bare hairline-divided stat list (no card chrome) two-column copy-left list-right with leading 01/02/03 mono indices; Method as three sequential rows with Step 0N mono left + title+body center + technical caption right (Sonnet 4.6 · parallel extraction · 8-way concurrency etc); Trust two-column with PdfMock + monospace bullet list of guarantees; FinalCta 12-col flush layout with clamp(48px,7vw,88px) headline left + CTA+caption right; real four-column SiteFooter with brand + Product/Built with/Author + bottom strip "© 2026 Chronicle · Not medical advice. Informational use only." Magic MCP usage: 1 inspiration call consumed (1/8 soft budget cap) on the "container scroll animation" 21st.dev component, adapted to framer-motion (project uses framer-motion not motion/react), then through three iterations on the reveal animation (pill-clip-morph → scale-fade lift → vertical curtain → final left-to-right wipe with constant 16px corners and offset ["start 0.7", "end 0.7"] tightly mapped over [0, 0.55] of scrollYProgress so the wipe completes by mid-section); 1040px max-width 16:10 panel with stylized clinician whiteboard inside (Sarah Chen header + jittered SVG axis with 6 date nodes + Levy/Park sticky notes with hand-drawn teal/amber arrows + 2 pinned PDF thumbnails with red push-pins + "T2D progression — 18 months" hand-feel title in bottom-left + "7 documents · 3 providers · 18 events" footer annotation in bottom-right). Browser tab title set ("Chronicle"), splash disclaimer wired with localStorage chronicle.splashDismissed.v1 flag, DisclaimerFooter mounted on /app + /eval, landing has self-contained SiteFooter with disclaimer in bottom strip. tsc --noEmit clean across all changes; next build green (now 11 routes — / + /app + /eval all static, 6 dynamic API routes including the new /api/cases/[id]/docs/[docId]); /eval cached fetch returns precomputed report in ms; /eval live SSE actually streams 8 case3 doc_started frames (held_out/case3 has been populated since cycle 14 — Path B is no longer fully active for Murat). [issues: drop path / live extract still not exercised against a real PDF in-browser (Murat task per BUILD.md H2 checkpoint); click-to-source pinpoint success rate unmeasured (Murat task per BUILD.md H6 ≥80% target across 8-10 events); H10 colorblind sim check unrun (Murat task — Chrome DevTools Deuteranopia/Protanopia, verify #DC2626 vs #991B1B stay distinguishable, swap urgent only to #7F1D1D if not); real /app screenshot for landing hero owed (currently using hand-built AppMockup as a high-fidelity placeholder — swap at H10/H11 polish); data/case3_eval_fallback.json is currently a placeholder per its own _placeholder:true flag — Murat captures the real Case 3 live result and overwrites at H11; Magic budget at 1/8 with 7 calls reserve]

Cycle 14 — frontend (H2 SSE wiring): replaced the fixture-direct paths in app/app/page.tsx with a real `POST /api/extract` SSE consumer per BUILD.md H2. New `lib/use-extraction-stream.ts` (reducer + AbortController) consumes both request shapes (preset → `{caseId}` JSON; drop → multipart `files[]`), parses SSE frames defensively (TimelineEventSchema.safeParse on every `event` frame so a malformed payload can't poison state), and applies the resolved-decision-#5 leaky bucket: `event` frames queue and drain on a 150ms cadence while doc-level frames (started/doc_started/doc_complete/doc_error/error/done) pass through immediately. Reducer keeps the events array date-sorted on every batch insert so Framer Motion's `layout` prop animates each card into its chronological slot. New UI surfaces in /app: in-flight progress strip ("N / total documents processed" + spinner), inline doc_error rows for skipped PDFs (per BUILD.md H5 spec, landed early because the error frame is already on the wire), and a top-level error banner that reads the standard envelope (humanizes case_not_extracted / pdf_invalid / extraction_failed / upstream_unavailable / rate_limit / network). Preset buttons disabled while extracting to avoid concurrent streams. lib/fixtures.ts no longer imported from /app — the FIXTURES re-export still exists for any future static rendering need but the runtime path is now SSE-only. Smokes (dev server up): case1 preset → 200 SSE, 7 doc_started + 18 events + 7 doc_complete + done over 10.6s wall (= 7×1.5s feel-delay confirming Q20 cadence honored end-to-end through the consumer); case3 preset → 404 envelope `{code:case_not_extracted}` in 5ms wall, ErrorBanner surfaces "Case not yet available" + the developer-facing message (Path B graceful-degrade verified through the UI layer); /app + / SSR both 200 with no console errors. tsc --noEmit clean; next build green (1605ms compile, 9/9 static pages, 5 dynamic API routes). [issues: drop path returns whatever Sonnet 4.6 actually emits — not yet exercised against a real PDF in this cycle (covered by H2 checkpoint when Murat drops a Case 1 PDF manually); H3 click-to-source + H4 timeline shell still owed; landing-hero Magic MCP polish still owed (Magic MCP tools are now in the registry, deferred per Murat's ordering)]

Cycle 16 — eval-integrity (Phase A, slice 1 of 3 — docs honesty/consistency pass, issue #7): reconciled the repo's held-out narrative with what the git/file timeline actually proves and published the real Case 3 numbers. Forensic timeline (normalized to UTC): prompt v1→v4 authored+scored on Cases 1+2 between 2026-05-10T06:24Z (v1 file) and 13:49:58Z (v4 scored, CHANGELOG, hash f32ebd0) — every CHANGELOG row references C1/C2 only, never C3; Case 3 GT `labeled_at` 2026-05-10T15:33:19Z; GT + `.gt_hash.lock` committed in 59ca076 at 2026-05-10T15:50:30Z; first recorded Case 3 measurement 2026-05-10T18:33:16Z (eval_runs filename/mtime), second 20:15:09Z. This makes the old README/CASES/BUILD/EVAL/page.tsx claim "labeled before any prompt iteration began" **false** (GT was authored ~2h AFTER v4 iteration finished). Strongest provable claim is L2, now used verbatim-consistently everywhere: "Case 3 was never used in prompt iteration; its ground truth was authored independently of model output on Case 3, hash-locked in commit 59ca076 before the prompt was ever run against Case 3 (first recorded measurement 2026-05-10T18:33:16Z)." Aggregate results read from eval_runs (peek discipline: aggregate blocks only, no per-event matched/missed): run1 18:33:16Z strict tp8/fp11/fn12 P0.42 R0.40 F1 0.41; run2 20:15:09Z strict tp9/fp11/fn11 P=R=F1 0.45; loose == strict in both (no date-fuzz recoveries); n_gt=20 both; both frozen prompt v4. README gained a "Held-out Case 3 results" section (both runs' strict+loose table, run-to-run-noise framing at n=20, and the dev→held-out gap rationale: Cases 1+2 gold was *derived from* AI-generated MOCK_DATA fixtures + their PDFs drafted from the same source → dev 0.825 partly measures round-trip fidelity of semi-synthetic docs; Case 3 is the only organically-authored, independently-labeled case, so 0.825→0.45 is expected, not a regression), plus a note that the escape-hatch rule (strict P or R < 0.5) fired on both runs. Aligned the ordering claim to L2 in docs/CASES.md:5, docs/EVAL.md:121, docs/BUILD.md:25 + :324, and held_out/case3/README.md (stale DEFERRED/Path-A-B status banner rewritten to LABELED/LOCKED/EVALUATED while keeping all anti-temptation STOP rules). Fixed fixture provenance in schema.md:180 + MOCK_DATA.md:5 ("hand-authored" → AI-generated during the planning session per STATE cycle 0; gold derived from those fixtures per cycle 7 — did not claim human review, unevidenced). app/eval/page.tsx: copy-only edits to the two overclaiming methodology strings (lines ~127 and ~565), no logic touched (git diff confirms text-only). data/case3_eval_fallback.json: replaced the `_placeholder:true` zeroed stub with the real 2026-05-10T20-15-09-892Z run — `.predicted`→`.events` (mechanical copy), `.metrics`→`.tiers`, and `.byEventType` **transposed** from the run's tier→type shape into the consumer's type→{strict,loose,n_gt} BreakdownRow shape (verified against data/eval_reports/case1.json + the page.tsx interfaces), promptVersion v4, `_placeholder` removed. Verify: `npx tsc --noEmit` clean; `npm test` 53/53; `npm run build` green (10/10 static pages); `npm run lint` exits 1 but that is the **pre-existing baseline** (1493 problems / 21 errors, all in untouched files — the only page.tsx entries are the pre-existing setState-in-effect errors at 206/345; my diff is JSX text only). Fallback render verified in a live browser at 375px (dev server relaunched with a dummy ANTHROPIC_API_KEY so /eval's auto-run could NOT perform a real Case 3 extraction): Cmd+Shift+L rendered strict+loose 45% cards (tp9/fp11/fn11/n_gt20) + all 7 transposed per-type rows (lab em-dash, imaging 100/50/67, visit 33/33/33, medication 0/0/0, procedure 100/100/100, referral 50/67/57) with 0 console errors; a throwaway node assertion also confirmed the JSON satisfies every consumer-destructured key. **Peek-budget bookkeeping: Case 3 scored measurements to date: 2 (2026-05-10, both prompt v4). Remaining budget: ≤2 scored measurement events (one event = up to 3 runs with mean reported); per-event error analysis not permitted.** [issues: (1) BLOCKER for the hotkey demo path — the consumer fetches `/data/case3_eval_fallback.json` but repo-root `data/` is NOT served by Next (returns HTTP 404 in a running server; no public/data, no rewrite, no route handler). Pre-existing wiring gap, out of scope here (data/docs/STATE only); needs a `public/` copy, a next.config rewrite, or a route handler in a later slice/orchestrator step. (2) held_out/case3/ground_truth.json reports n_events=22 but eval n_gt=20 — most likely the EVAL.md `in_scope:false` exclusion (protocol requires 2-3 such events; 22−2=20), i.e. designed behavior not a bug; NOT confirmed, did not open the GT events per the held-out STOP rule. (3) docs/CASES.md:3 still says Cases 1+2 PDFs are "authored by hand" which conflicts with the semi-synthetic framing (LLM-drafted); left as-is, out of scope for this slice. (4) app/eval/page.tsx JSDoc header (lines 8-9) still says "Path B is currently active" — stale but it is a code comment, not copy, so left untouched per scope.]

Cycle 17 — eval-integrity (Phase A, slice 2 of 3 — measurement rigor, issue #7): added deterministic-as-possible extraction + a held-out Case 3 N-run measurement CLI + usage/cost accounting + CI, and closed slice-1 BLOCKER #1 (fallback serving). **Temp 0 (task A):** set `temperature: 0` on the extraction Messages call in lib/claude.ts. Context7/Anthropic-docs verification surfaced a live gotcha: the SDK marks `temperature` `@deprecated` and models released AFTER Claude Opus 4.6 (Opus 4.7+, Sonnet 5, Fable/Mythos 5) reject any non-default value with HTTP 400 — but `claude-sonnet-4-6` (the extraction model) is EXCLUDED and still accepts it (migration-guide + working-with-messages confirm). Left a load-bearing comment: if the model is ever bumped past that line the field must be removed. All historical CHANGELOG rows + the two 2026-05-10 Case 3 runs predate this and were at API default temp (≈1.0, no seed) — documented in EVAL.md §6 + README. **N-run CLI (task B):** scripts/eval-case3.ts (makes EVAL.md:150 true) — `--runs N` (default 3) sequential extract+eval passes, each persisted to held_out/case3/eval_runs/<ts>.json in the existing eval_run shape (+usage), plus a printed+persisted summary-<ts>.json with mean/min/max strict+loose P/R/F1 and per-run tp/fp/fn (summarizeRuns). Reuses extractDocWithUsage + lib/eval; predicted flattened in DOC order (Promise.all preserves input order → deterministic). Enforces held-out hygiene (gt_hash.lock == git hash-object GT, prompts git-clean, writes prompt_hash.txt) for case3; a dev mode (`eval-case3 case1 …` reading data/cases/<id>) runs the identical machinery WITHOUT gates for testing. **DID NOT execute against Case 3** (peek budget untouched; still 2 scored to date, ≤2 remaining). **Usage persistence (task C):** refactored lib/claude.ts → extractDocWithUsage returns {events, usage}; extractDoc is a thin wrapper (5 existing callers unchanged). Four fields (input/output/cache_read/cache_creation) normalized (null→0) via new lib/measure.ts normalizeUsage; persisted to metadata.json (usageTotals + perDoc[].usage in extract-case.ts) and eval_runs (usage + perDocUsage in eval-case3 + app/api/eval/route.ts). Old artifacts stay readable (metadata consumer reads only generatedAt/modelVersion; cache-report skips no-usage files). Console log reformatted to fixed fields `[claude] doc=… input=… output=… cache_read=… cache_create=…` (was `usage=%j`). **cache-report (task D):** scripts/cache-report.ts scans metadata.json + eval_runs (top-level usage only for held_out — never per-event predicted) → total tokens, % input served from cache, net $ saved vs no-caching incl. 1.25× write premium, priced from new lib/pricing.ts (dated 2026-07-22 table: sonnet-4-6 $3/$15 in/out, 5m-write $3.75=1.25×, read $0.30=0.1×, verified against platform.claude.com pricing). Graceful "no usage yet" when nothing carries usage (current repo state). **Serving fix (task E):** new route app/api/eval/fallback/route.ts reads the ONE canonical data/case3_eval_fallback.json; changed page.tsx loadFallback fetch `/data/case3_eval_fallback.json`→`/api/eval/fallback` (single source of truth, no public/ copy, no rewrite). **CI (task F):** .github/workflows/ci.yml on pull_request + push:main, permissions contents:read, concurrency cancel-in-progress, Node 22 LTS (no .nvmrc/engines), npm ci → blocking npx tsc --noEmit / npm test / npm run build, lint informational (continue-on-error + TODO to make blocking after the 21-error baseline is cleaned). **Tests (task H):** lib/measure.test.ts (+9: normalizeUsage null/missing coercion, sumUsage, summarizeRuns mean/min/max + empty guard, assembleRunRecord doc-order flatten + usage-shape + N-run→summarize) and lib/pricing.test.ts (+5: hand-computed with/no-cache math, negative-savings cold-write, zero-usage no-NaN, unknown-model throws). Verify: `npx tsc --noEmit` clean; `npm test` 69/69 (was 53); `npm run build` green (11 routes incl new /api/eval/fallback); `npm run lint` at baseline 1493 problems/21 errors (removed my one stray unused-import warning; the 2 page.tsx errors are the pre-existing setState-in-effect ones, shifted +2 lines by my JSDoc edit). **Machinery test path = MOCKED (task B option b):** the ANTHROPIC_API_KEY in .env.local is INVALID (real 108-char sk-ant- key returns 401 authentication_error) so the live case1 `--runs 2` real-API check could not run; instead covered the N-run machinery with mocked unit tests (assembleRunRecord + summarizeRuns) + a scratchpad harness that replicated eval-case3's write loop with a mock extractor, proving the persisted run JSON keys [generatedAt,caseId,promptHash,temperature,run,runs,predicted,metrics,byEventType,usage,perDocUsage] + summary shape; and dry-run against both case1 (7 docs) and case3 (8 docs) proved CLI plumbing. cache-report positive path verified by seeding a throwaway data/cases/case1/eval_runs with the harness runs (17,082 in / 13,704 read / 13,704 write / 860 out → 30.8% from cache → $0.1196 vs $0.1464 = $0.0267 saved / 18.3%), then deleted (git clean). **Live fallback render (task E):** dev server on :3210 with ANTHROPIC_API_KEY="" (empty overrides .env.local per @next/env → getClient throws pre-network, so NO Case 3 PDF is transmitted — stronger than a dummy key); GET /api/eval/fallback = HTTP 200 real JSON; Playwright @375px navigate /eval + dispatch Cmd+Shift+L → rendered strict+loose 45% cards (tp9/fp11/fn11/n_gt20) + all 7 per-type rows (imaging 100/50/67, visit 33/33/33, diagnosis 75/60/67, medication 0/0/0, procedure 100/100/100, referral 50/67/57, lab —) tagged "Cached fallback", 0 console errors (2 msgs = React-devtools hint + HMR); screenshot at .playwright-mcp/eval-fallback-375px.png. The mount auto-run wrote one spurious EMPTY eval_run (all 8 docs threw "ANTHROPIC_API_KEY required", predicted=[], tp0, zero usage — no model contact, no measurement) which I deleted, restoring eval_runs to the identical pre-check 3 files. **Docs (task G):** EVAL.md §6 "Held-out measurement protocol" (temp-0 + model caveat + historical-numbers-predate note; measurement event = 3 runs mean±range; peek budget ≤2 remaining, no per-event analysis; eval-case3 usage; usage persistence); BACKEND-STANDARDS.md reopened (footer notes it) — §J.8 console format updated + new §J.11 (temp-0 + persisted usage table); README Scripts gained eval-case3 + cache-report + a temp-0/usage paragraph, and the Case 3 results section gained a temp-0-predates note. [issues: (1) **.env.local ANTHROPIC_API_KEY is invalid (401)** — blocked the real-API machinery check; orchestrator should refresh the key if a real case1 `--runs 2` smoke is wanted before merge (mocked coverage + harness stand in). (2) tsx is NOT a declared dep (not in package-lock) — `npm test` relies on `npx tsx` auto-installing latest at runtime; works in non-interactive CI but is non-hermetic. Left as-is per smallest-diff; adding `tsx` as a pinned devDependency would make CI deterministic (orchestrator call — touches package.json+lock). (3) the /eval mount auto-run writes an eval_run every load (pre-existing app/api/eval behavior, gitignored) — with a VALID key each demo /eval visit is effectively a Case 3 run; not changed here, but worth noting for peek-budget discipline during rehearsal. (4) slice-1 issue #2 (n_events 22 vs n_gt 20) and #3/#4 (CASES.md:3 "authored by hand"; page.tsx stale JSDoc "Path B active") remain out of scope — untouched.] **Fix pass (post-vet, same uncommitted slice-2 batch):** vetting found one untracked/gitignored artifact — `held_out/case3/eval_runs/2026-07-23T06-21-47-086Z.json` — sitting beside the 2 canonical runs. Forensics (from its STRUCTURE only, no canonical-run peeking): written by the PRE-EXISTING `/api/eval` live auto-run during slice-1's fallback-render check — the dev server had a dummy `ANTHROPIC_API_KEY`, so every per-doc extraction threw, the route caught them and scored `predicted:[]` against the real GT and persisted it anyway. Confirmed by shape: empty `predicted`, tp0/fn20/n_gt20, real promptHash f32ebd0, `usage`/`perDocUsage` ABSENT (the pre-slice-2 run shape → predates `scripts/eval-case3.ts`), no `summary-*.json` sibling. Zero successful model calls → zero information about model performance → NOT a held-out measurement, not billed, does NOT count against the peek budget ("2 scored to date, ≤2 remaining" unchanged). Removed (`rm`; untracked). GUARD added so it can never recur: `isDegenerateRun(outcomes)` in lib/measure.ts (true when 0 docs extracted successfully) enforced in BOTH persist paths — app/api/eval/route.ts live mode emits an `all_docs_failed` SSE error frame instead of writing; scripts/eval-case3.ts now catches per-doc failures and aborts non-zero BEFORE writing any run file (and, in an N-run sequence, before the summary). A run with ≥1 success MAY persist but records failed docs explicitly (`RunRecord.docFailures`). Verified end-to-end on the dev case (case1, empty key → all 7 docs fail pre-network → abort exit 1, scratch runs-dir left empty — no run, no summary). +6 measure unit tests (isDegenerateRun empty/all-fail/partial/all-ok; assembleRunRecord docFailures partial+empty) → `npm test` 75/75 (was 69). Same batch closed the remaining riders: **tsx pinned** as exact devDependency `4.23.1` (slice-2 issue #2 — package.json+package-lock; test script `npx tsx`→`tsx` so `npm test`/CI resolve the LOCAL install, hermetic; ci.yml unchanged — `npm ci` covers it) and the **copy strays** — docs/CASES.md:3 ("authored by hand" → semi-synthetic: Cases 1+2 LLM-drafted from planning material, Case 3 organically authored), app/eval/page.tsx JSDoc L8-9 ("Path B active" → Case 3 labeled/locked/evaluated) + the gt_not_present error copy tag ("Path B — held-out deferred" → honest "Held-out data unavailable") + a new `all_docs_failed` error case (slice-1 issues #3/#4). Docs: EVAL.md §6 gained a "Degenerate runs are not measurements (never persisted)" subsection carrying this forensic note. Re-verify: `npx tsc --noEmit` clean; `npm test` 75/75; `npm run build` green (11 routes); `npm run lint` at baseline 1493 problems/21 errors (0 new — the only page.tsx entries are the pre-existing setState-in-effect errors, now at 208/347). `held_out/case3/eval_runs/` back to exactly the 2 canonical files (2026-05-10T18:33:16Z + 20:15:09Z). Still open: slice-2 issue #1 (.env.local key returns 401 — orchestrator to refresh before any real Case 3 run) and slice-1 issue #2 (GT n_events 22 vs n_gt 20, most likely the in_scope:false exclusion, unconfirmed per the held-out STOP rule).

